# Copy to .env.local and fill in. Server-side only (no NEXT_PUBLIC_ prefix).
# Any Solana devnet RPC works; a dedicated one avoids public-endpoint throttling.
RPC=https://devnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY
